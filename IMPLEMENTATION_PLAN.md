# Implementation Plan

This file is rewritten by the plan-mode loop (`./loop.sh -m plan -n 3`) from the newest spec
in `specs/`. It is empty until Step 1 / Stage 3 runs.

- [ ] (no tasks yet — run the plan loop after specs/base.md exists)

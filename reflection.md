- I believe that, throughout this assignment, I could see how the loop behaved, drawing some conclusions in the process. The first feature that I'd like to mention something about is the loop's autonomy. In the specification, as commented in class, the definition of "done" is quite strict, and whenever there was something wrong or imprecise, the agent simply stopped and I had to either correct it by hand or give it some ideas about how to proceed from there. Even though I thought that this would progress well, the development generated a bug that I spent a couple of hours trying to tackle (a rejected API turn silently advancing the RNG and shifting every subsequent damage roll). The strategy that worked to solve this issue was to make the model show its rationale while manually tracing it and asking questions. Another caveat that I'd like to mention is that task 4.2 showed me a different limit. The AI-strength tests simply could not be made to pass, and rather than fudge the threshold, that failure surfaced two things that reviewing the spec never would have: a real defect in my own design, which modeled the opponent as an adversary and therefore lost 31/69 to a simple heuristic, and acceptance criteria that I had written as unmeasured guesses (documented in E13).